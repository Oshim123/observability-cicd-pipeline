These screenshots show proof that the monitoring system is actually working.

- cloudwatch_cpu.png → shows CPU usage increasing during the CPU stress test
- cloudwatch_logs.png → shows Flask logs being sent to CloudWatch
- grafana_dashboard.png → shows metrics visualised in Grafana in real time
- alarms.png → shows CloudWatch alarm configuration

Basically, this is visual proof that the system is detecting what’s going on after deployment.
